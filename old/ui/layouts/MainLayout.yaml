imports:
  - import { Route } from 'svelte-router-spa'
props:
  currentRoute:
    type: any
state:
  params:
    type: object
tmpl: >
  <div class="app">
    <main>
      <Route {currentRoute} {params} />
    </main>
  </div>
