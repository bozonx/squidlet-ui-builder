template:
  - type: Element
    tag: div
    props:
      class:
        type: string
        value: w-full h-screen
    children:
      - type: Element
        tag: nav
        props:
          class:
            type: string
            value: flex w-full h-48px bg-gray-800
        children:
          - type: Element
            tag: div
            props:
              class:
                type: string
                value: w-60
            children:
              - type: Text
                value: TopBar
      - type: Element
        tag: div
        props:
          class:
            type: string
            value: workarea
        children:
          - type: RouterView
stylesScoped: |
  .workarea {
    display: flex;
    flex-direction: row;
    height: calc(100vh - 48px);
  }