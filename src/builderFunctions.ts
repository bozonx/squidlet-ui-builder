export function copyBaseProject(buildDir: string) {
  // TODO: копируем базу проекта в директорию сборки
}

export function installDependencies(buildDir: string) {
  // TODO: запускаем установку зависимостей
}

export function buildFiles(buildDir: string, translator: string, indexFile: string) {
  // TODO: do it
}