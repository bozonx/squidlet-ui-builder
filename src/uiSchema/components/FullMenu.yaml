
data:
  - routes:
      resource: menuRoutes
      # you cad add some request params here

tmpl: '''
  <ul>
    <Load resource={routes}>
      <li>
        <MenuItemRoute item={item} />
      </li>
    </Load>
  </ul>
  '''
