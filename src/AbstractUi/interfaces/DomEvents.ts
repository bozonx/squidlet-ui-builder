export enum OutcomeEvents {
  mount,
  unMount,
  update,
}

export enum IncomeEvents {
  click,
  // messenger events
  sendText,
  sendPhoto,
  sendVideo,
  sendAudio,
  sendPoll,
  // full-functional UI events
  keyPressed,
  keyDown,
  focus,
  blur,
  mouseMove,
  mouseEnter,
  mouseLeave,
  middleClick,
  rightClick,
  backClick,
  swipe,
}
