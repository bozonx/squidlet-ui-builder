imports:
  - 'import {ROOT_PATHS} from "../lib"'

tmpl: >
  <ul>
    <SLocalDataDirContainer path={ROOT_PATHS.screens} let:list={list}>
      <div>{list}</div>
    </SLocalDataDirContainer>
  </ul>

#      <SLoad let:list={list} pending={!$list.initialized}>
#        {#each fileName of list.data}
#          <li>
#
#            <LocalDataFileContainer path={`${ROOT_PATHS.screens}/${fileName}`}>
#              <SLoad pending={!$item.initialized}>
#                <MenuItemScreen item={$item} />
#              </SLoad>
#            </LocalDataFileContainer>
#
#          </li>
#        {/each}
#      </SLoad>
