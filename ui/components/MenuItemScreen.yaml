props:
  item:
    type: MenuScreens
    required: true
  test:
    type: number
    default: 4
state:
  localState:
    type: string
    default: 'str'
  #- item:
  #    schema: RouteItem
# TODO: add schema
tmpl: >
  <div>
    menu item {test} {localState} {item.name}
  </div>
